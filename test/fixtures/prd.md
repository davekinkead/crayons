# PRD: Add Foo Class Method

## Goal
Create a `Foo` class with a class method that returns "bar".

## Requirements
- Create a class named `Foo`
- Add a class method `.foo` that returns the exact string "bar"
- Follow Ruby conventions

## Acceptance Criteria
- Test `Foo.foo` returns "bar"
- All tests pass
